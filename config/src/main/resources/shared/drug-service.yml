spring:
  data:
    mongodb:
      host: drug-mongodb
      username: user
      password: ${MONGODB_PASSWORD}
      database: clinical-management
      port: 27017

server:
  context-path: /drugs
  port: 7500